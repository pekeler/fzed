name: release_nightly

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain disabled upstream nightly release workflow
        run: |
          echo "The inherited upstream Zed nightly release workflow is disabled in FZed."
          echo "FZed does not publish nightly binaries yet."
          echo "Add a FZed-owned nightly release workflow before enabling automation."
