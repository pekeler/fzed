name: publish_extension_cli

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain disabled upstream extension CLI publishing workflow
        run: |
          echo "The inherited upstream Zed extension CLI publishing workflow is disabled in FZed."
          echo "FZed does not publish extension CLI artifacts yet."
