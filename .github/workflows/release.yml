name: release

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain disabled upstream release workflow
        run: |
          echo "The inherited upstream Zed release workflow is disabled in FZed."
          echo "FZed currently publishes manual GitHub releases without binary assets."
          echo "Add a FZed-owned release workflow before enabling automated release builds."
