name: after_release

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain disabled upstream after-release workflow
        run: |
          echo "The inherited upstream Zed after-release workflow is disabled in FZed."
          echo "It references upstream release pages, Discord, Sentry, and winget automation."
          echo "Add FZed-owned post-release automation before enabling this workflow."
