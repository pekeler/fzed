name: deploy_collab

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain disabled upstream collab deployment workflow
        run: |
          echo "The inherited upstream Zed collab deployment workflow is disabled in FZed."
          echo "FZed does not own upstream Zed's collab deployment infrastructure."
